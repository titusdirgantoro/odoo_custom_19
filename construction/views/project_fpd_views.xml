<odoo>
    <!-- Action -->
    <record id="action_project_fpd" model="ir.actions.act_window">
        <field name="name">FPD</field>
        <field name="res_model">project.fpd</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Tree -->
    <record id="view_project_fpd_tree" model="ir.ui.view">
        <field name="name">project.fpd.tree</field>
        <field name="model">project.fpd</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="rap_id"/>
                <field name="project_id"/>
                <field name="pekerjaan_id"/>
                <field name="sub_pekerjaan_id"/>
                <field name="state"/>
                <field name="amount_total" widget="monetary" options="{'currency_field': 'currency_id'}"/>
            </list>
        </field>
    </record>

    <!-- Form -->
    <record id="view_project_fpd_form" model="ir.ui.view">
        <field name="name">project.fpd.form</field>
        <field name="model">project.fpd</field>
        <field name="arch" type="xml">
            <form string="FPD">
                <header>
                    <button name="action_confirm" type="object" string="Confirm" class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve" class="oe_highlight"
                            invisible="state != 'confirm'"/>

                    <button name="action_set_draft" type="object" string="Set Draft"
                            invisible="state not in ('confirm','approve','cancel')"/>

                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state == 'cancel'"/>

                    <field name="state" widget="statusbar" statusbar_visible="draft,confirm,approve,cancel"/>
                </header>

                <sheet>
                    <group>
                        <group>
                            <field name="name" readonly="1"/>
                            <field name="rap_id" readonly="0"/>
                            <field name="project_id" readonly="0"/>
                        </group>
                        <group>
                            <field name="pekerjaan_id" readonly="0"/>
                            <field name="sub_pekerjaan_id" readonly="0"/>
                            <field name="amount_total" widget="monetary"
                                   options="{'currency_field': 'currency_id'}" readonly="0"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Lines">
                            <field name="line_ids" readonly="state == 'approve'">
                                <list editable="bottom">
                                    <field name="product_tmpl_id" domain="allowed_product_tmpl_ids and [('id','in', allowed_product_tmpl_ids)] or []"/>
                                    <field name="type_master_data" readonly="1"/>
                                    <field name="qty"/>
                                    <field name="uom_id"/>
                                    <field name="price_unit"/>
                                    <field name="total_price" readonly="1"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>
